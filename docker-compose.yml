version: '3'

services:

  gitea:
    image: gitea/gitea:latest
    ports:
      - "3000:3000"
    volumes:
      - ./gitea_data:/data

  jenkins:
      image: jenkins/jenkins:lts
      restart: always
      privileged: true
      user: root
      ports:
        - "9090:8080"
        - "50000:50000"
      volumes:
        - ./jenkins_home:/var/jenkins_home
        - /var/run/docker.sock:/var/run/docker.sock
        - /usr/bin/docker:/usr/bin/docker          # Maps the docker command
        - /usr/bin/docker-compose:/usr/bin/docker-compose # Maps the compose command

  backend:
    image: dockerdemo
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/myuser
      SPRING_DATASOURCE_USERNAME: myuser
      SPRING_DATASOURCE_PASSWORD: secret

  frontend:
    image: dockerdemoweb
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "4200:80"

  db:
    image: 'postgres'
    container_name: db
    environment:
      - POSTGRES_USER=myuser
      - POSTGRES_PASSWORD=secret