# OTel Collector — Dev configuration
# Receives OTLP from backend Java Agent + Traefik,
# scrapes Traefik Prometheus metrics,
# exports traces, metrics, and logs to Splunk HEC + debug stdout.

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

  prometheus:
    config:
      scrape_configs:
        - job_name: traefik
          scrape_interval: 30s
          static_configs:
            - targets: ["traefik_traefik:8082"]

  postgresql:
    endpoint: myapp_db:5432
    username: monitor
    password: monitor
    databases:
      - myuser
    collection_interval: 30s
    tls:
      insecure: true

processors:
  memory_limiter:
    limit_mib: 600
    spike_limit_mib: 150
    check_interval: 1s

  resourcedetection:
    detectors: [system, env]
    system:
      hostname_sources: [os]
    timeout: 5s
    override: false

  batch:
    timeout: 1s
    send_batch_size: 8192

exporters:
  debug:
    verbosity: basic

  splunk_hec:
    token: "otel-hec-token"
    endpoint: "http://splunk_splunk:8088"
    source: "otel"
    sourcetype: "otel"
    index: "main"
    log_data_enabled: true
    profiling_data_enabled: false
    tls:
      insecure_skip_verify: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [memory_limiter, resourcedetection, batch]
      exporters: [debug, splunk_hec]
    metrics:
      receivers: [otlp, prometheus, postgresql]
      processors: [memory_limiter, resourcedetection, batch]
      exporters: [debug, splunk_hec]
    logs:
      receivers: [otlp]
      processors: [memory_limiter, resourcedetection, batch]
      exporters: [debug, splunk_hec]

  telemetry:
    logs:
      level: info
