global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # OTel Collector exposes all metrics (app + traefik + postgres) via prometheus exporter
  - job_name: otel-collector
    scrape_interval: 15s
    static_configs:
      - targets: ["otel_otel-agent:8889"]
